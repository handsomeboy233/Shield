[FEATURES]
features:length,params_number,return_code,size,upper_cases,lower_cases,special_chars,url_depth,user_agent,http_query,ip

[LOG]
apache_regex:([(\d\.)]+) - - \[(.*?)\] "(.*?)" (\d+) (.+) "(.*?)" "(.*?)"
apache_names:["ip","date","query","code","size","referrer","user_agent"]

nginx_regex:([(\d\.)]+) - - \[(.*?)\] "(.*?)" (\d+) (\d+) (.+) "(.*?)" "(.*?)"
nginx_names:["ip","date","query","code","size","referrer","user_agent"]

http_regex:^(\d*?\.\d*?)\t.*?\t(.*?)\t.*?\t.*?\t.*?\t.*?\t(.*?\t.*?\t.*?\t.*?)\t(.*?)\t.*?\t(.*?)\t(.*?)\t.*$
http_names:["date","ip","query","user_agent","size","code"]

apache_error:
nginx_error:

[PROCESS_DETAILS]
attributes:['status', 'num_ctx_switches', 'memory_full_info', 'connections', 'cmdline', 'create_time', 'num_fds', 'cpu_percent', 'terminal', 'ppid', 'cwd', 'nice', 'username', 'cpu_times', 'memory_info', 'threads', 'open_files', 'name', 'num_threads', 'exe', 'uids', 'gids', 'memory_percent', 'environ']

[CVE]
source:https://services.nvd.nist.gov/rest/json/cves/2.0?keywordSearch=
year_threshold:YYYY

[LLM]
# Use the following URL if using docker compose services
# url:http://ollama:11434
url:http://192.168.31.37:11434
# Use the following URL if NOT using docker compose services
#url:http://127.0.0.1:11434
model:gemma3:4b
prompt:You are a cybersecurity analyst.Analyze the following web security finding and return ONLY a valid JSON object.Do not include markdown.Do not include code fences.Do not include any explanation before or after the JSON.The JSON object must contain exactly these keys:- potential_attack_attempt- details- severity- CVE- recommendation- owasp Rules:- potential_attack_attempt must be boolean: true or false- severity must be one of: "low", "medium", "high"- CVE must be an array- owasp must be an array- details must be a string- recommendation must be a stringExample output:{"potential_attack_attempt": true,"details": "The request contains SQL metacharacters and appears to be a possible SQL injection attempt.","severity": "high","CVE": [],"recommendation": "Use parameterized queries, validate user input, and review database logs for suspicious access.","owasp": ["A03:2021-Injection"]}Now analyze this finding:<FINDING_HERE>.Return ONLY raw JSON. Do not use markdown code fences.

[WEBAPP]
# Use the following url if using docker compose services
url:http://webhawk_ui:8080/api/v1/incidents
# Use the following URL if NOT using docker compose services
#url:http://127.0.0.1:8080/api/v1/incidents
